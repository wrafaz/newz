import React, {Component} from 'react';
import ReactDOM from 'react-dom';

class HeaderContent extends Component {

    render()
    {
        return(
            <header className="headerContent">Header Menu</header>
        );
    }
}

export default HeaderContent;